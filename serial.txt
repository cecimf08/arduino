Enviand dados para porta serial
int pushButton = 2;
void setup () {
Serial.begin (9600);
// make the pushbutton's pin an input: pinMode (pushButton, INPUT) ;
void loop() {
// read the input pin:
int buttonState = digitatRead (pushButton) ;
//print out the state of the button:
Serialprintln(buttonstate);
// delay in between reads for stability
delay ( 100);
｝
